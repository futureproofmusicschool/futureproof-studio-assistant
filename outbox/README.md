# Outbox

Email drafts written by the assistant during a voice session in the Talk tab.

**Nothing here has been sent.** `draft_email` only ever writes a file. Read the draft, edit it if you want, and send it yourself. When a draft goes out, log it against the contact in `contacts/contacts.json` and delete or archive the file.
