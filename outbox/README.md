# Outbox

Email drafts written by TEO during a voice session in the Talk tab.

**Nothing here has been sent.** `draft_email` only ever writes a file. John reads the draft, edits it if he wants, and sends it himself from Gmail. When a draft goes out, log it against the contact in `contacts/contacts.json` and delete or archive the file.
